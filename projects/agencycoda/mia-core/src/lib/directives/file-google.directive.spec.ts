import { FileGoogleDirective } from './file-google.directive';

describe('FileGoogleDirective', () => {
  it('should create an instance', () => {
    const directive = new FileGoogleDirective();
    expect(directive).toBeTruthy();
  });
});
